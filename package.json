{
  "name": "claude-code-standalone-cli",
  "version": "1.0.0",
  "description": "Standalone CLI for running Claude Code on GitHub/GitCode issues and PRs",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.145"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
